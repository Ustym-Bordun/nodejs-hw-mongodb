description: UnauthorizedError - Token is expired or invalid
content:
  application/json:
    schema:
      $ref: ../../schemas/auth/errors/resetTokenExpiredOrInvalid.error.yaml
    examples:
      resetTokenExpired:
        $ref: ./examples/resetTokenExpired.auth.example.yaml
      resetTokenInvalid:
        $ref: ./examples/resetTokenInvalid.auth.example.yaml
