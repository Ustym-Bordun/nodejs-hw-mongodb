description: Creates a contact
content:
  application/json:
    schema:
      $ref: ../../schemas/contacts/success/postContact.success.yaml
    examples:
      createContact:
        summary: Create a contact
        $ref: ./examples/postContact.contacts.example.yaml
